Konteks penting: Gue udah add 7 secret di Replit Secrets kemarin 
(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, MAILTRAP_API_URL, 
MAILTRAP_SENDER_EMAIL, MAILTRAP_SENDER_NAME, APP_URL, VITE_APP_URL). 
Total secret di Replit sekarang 14 (7 lama + 7 baru). 

Lu salah saat bilang "Belum di-add" — itu false alarm karena lu gak cek 
actual environment variables.

Sekarang verify secret-nya BENERAN ke-load. Lakuin ini:

1. Run di shell Replit:
   
   node -e "console.log({
     SUPABASE_URL: !!process.env.SUPABASE_URL,
     SUPABASE_SERVICE_ROLE_KEY: !!process.env.SUPABASE_SERVICE_ROLE_KEY,
     API_Tokens_Mailtrap: !!process.env.API_Tokens_Mailtrap,
     MAILTRAP_API_URL: !!process.env.MAILTRAP_API_URL,
     MAILTRAP_SENDER_EMAIL: !!process.env.MAILTRAP_SENDER_EMAIL,
     MAILTRAP_SENDER_NAME: !!process.env.MAILTRAP_SENDER_NAME,
     APP_URL: !!process.env.APP_URL,
   })"
   
   Expected output: semua harus 'true'. Kalau ada yang 'false', kasih tau 
   gue mana yang false.

2. Verify lazy load di backend code masih works:
   - Buka artifacts/api-server/src/lib/supabase-admin.ts
   - Pastikan supabase client di-instantiate lazy (cuma pas dipanggil, 
     bukan saat module load) supaya server gak crash kalau secret missing
   - Kalau masih eager-load (top-level new SupabaseClient(...)), refactor 
     jadi factory function getSupabaseAdmin()

3. Test actual auth endpoint dari Replit shell (bukan cuma healthz):
   
   curl -X POST http://localhost:8080/api/auth/magic-link/request \
     -H "Content-Type: application/json" \
     -d '{"email":"test-noop@example.com"}'
   
   Expected: HTTP 200 dengan { "ok": true } — karena email gak terdaftar, 
   tapi response tetap success untuk anti-enumeration. 
   
   Kalau dapet 500 dengan error "SUPABASE_URL is not defined" atau 
   "Mailtrap env vars missing" → ada secret typo/missing.
   Kalau dapet 500 dengan error Mailtrap auth failure (token invalid) → 
   secret format/value salah.
   Kalau dapet 200 → secret bener semua, email kirim ke Mailtrap berhasil.

4. Kasih full output:
   - Hasil dari step 1 (env var check)
   - Hasil curl di step 3 (status code + response body)
   - Kalau ada error di log API server pas curl jalan, copy log-nya